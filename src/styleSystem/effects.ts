interface Effects {
    shadow: {
        defaultCard: string;
    }
}

const effects: Effects = {
    shadow: {
        defaultCard: '0px 4px 8px rgba(0, 0, 0, 0.1)',
    }
}

export default effects;